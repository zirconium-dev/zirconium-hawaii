for i in /etc/profile.d/*.sh; do
  if [ -r "${i}" ]; then
    if [ "${-#*i}" != "$-" ]; then
      . "${i}"
    else
      . "${i}" >/dev/null
    fi
  fi
done
unset i

if [ "${PS1-}" ] && [ -n "${BASH_VERSION}" ]; then
  PS1='[\u@\h \W]\$ '
  export PS1
fi
